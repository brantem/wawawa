export type Stream = {
  id: string;
  group: string;
  title: string;
  filename: string;
  seeders: string;
  size: string;
  origin: string;
};

export type Subtitle = {
  id: string;
  url: string;
  label: string;
};
